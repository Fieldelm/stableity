type language = {
    code: string
    name: string,
    country_code: string
}

type Page = {
    name: string,
    route: string,
    element: JSX.Element
  }

type User = {
    email: string,
    password : string
}
